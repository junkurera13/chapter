{
  "name": "SidequestUser",
  "type": "object",
  "properties": {
    "auth_user_id": {
      "type": "string",
      "description": "Base44 authenticated user ID for the private app account"
    },
    "email": {
      "type": "string",
      "description": "Verified email supplied by Base44 authentication"
    },
    "phone": { "type": "string", "description": "E.164 phone number" },
    "first_seen_at": { "type": "number" },
    "country": { "type": "string" },
    "name": { "type": "string" },
    "home_city": { "type": "string" },
    "current_city": { "type": "string" },
    "on_vacation": { "type": "boolean" },
    "introductions_muted": {
      "type": "boolean",
      "description": "Set only when someone has asked to stop being shown people they have not met. Absent means they take part, because a gist about a stranger is made only of what the reader already holds and discloses nothing about either of them."
    },
    "introductions_muted_at": { "type": "number" },
    "home_city_key": {
      "type": "string",
      "description": "Normalised home_city, so two people who wrote their city differently are still in one place. The introduction pool is queried on this."
    },
    "notes": { "type": "string" },
    "memory_updated_at": { "type": "number" },
    "eve_session_id": {
      "type": "string",
      "description": "Durable Eve conversation session id"
    },
    "eve_continuation_token": {
      "type": "string",
      "description": "Opaque Eve continuation token for the user's shared Chapter conversation"
    },
    "eve_stream_index": {
      "type": "number",
      "description": "Last consumed Eve event index"
    },
    "latitude": { "type": "number" },
    "longitude": { "type": "number" },
    "signed_up_at": { "type": "number" },
    "assigned_phone": { "type": "string" },
    "onboarding_step": {
      "type": "string",
      "enum": [
        "needs_memory_invite",
        "awaiting_memory",
        "memory_ready"
      ]
    }
  },
  "required": ["first_seen_at"],
  "rls": {
    "create": { "user_condition": { "role": "admin" } },
    "read": { "user_condition": { "role": "admin" } },
    "update": { "user_condition": { "role": "admin" } },
    "delete": { "user_condition": { "role": "admin" } }
  }
}
